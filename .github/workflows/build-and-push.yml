name: Build, Push & Deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - 'k8s/**'
  pull_request:
    branches: [main]

jobs:
  deploy:
    uses: t-tibor/shared-workflows/home-bot-shared-workflow.yml@main
    permissions:
      contents: write
      packages: write
